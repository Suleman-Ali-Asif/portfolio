import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "another-domain.net"], // Add your external image domains here
  },
};

export default nextConfig;
